package champion

import "context"

type LoaderData struct {
}

templ Loader(ctx context.Context, data *LoaderData) {
	<div
		hx-get="/profile/web/partial/champion"
		hx-trigger="load once"
	>
		// TODO: hx-get
		<div class="h-40"></div>
	</div>
}
