package ladder

import "context"

type TableData struct {
	Region string
	Rows   []RowData
}

templ Table(ctx context.Context, data TableData) {
	<div class="relative overflow-x-auto rounded-xl border border-default">
		<table class="w-full rounded-lg text-left">
			<thead class="rounded-lg border-b text-xs bg-neutral-secondary border-default text-body-subtle">
				<tr>
					<th scope="col" class="px-6 py-3 font-medium">
						Position
					</th>
					<th scope="col" class="px-6 py-3 font-medium">
						Summoner
					</th>
					<th scope="col" class="px-6 py-3 font-medium">
						Rank
					</th>
					<th scope="col" class="px-6 py-3 font-medium">
						Win-Loss
					</th>
				</tr>
			</thead>
			<tbody class="text-sm text-body">
				for _, data := range data.Rows {
					@Row(ctx, data)
				}
			</tbody>
		</table>
	</div>
}
