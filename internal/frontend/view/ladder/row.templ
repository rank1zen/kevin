package ladder

import (
	"context"
	"fmt"
)

type RowData struct {
	Place         int
	Name, Tag     string
	Tier          string
	Division      string
	LP            int
	Wins, Losses  int
	WinPercentage int
}

templ Row(ctx context.Context, data RowData) {
	<tr class="bg-neutral-primary even:bg-neutral-secondary hover:bg-neutral-tertiary">
		<td class="px-6 py-4 whitespace-nowrap text-sm font-semibold text-body">
			{ data.Place }
		</td>
		<td class="px-6 py-4 whitespace-nowrap text-sm text-body">
			{ fmt.Sprintf("%s#%s", data.Name, data.Tag) }
		</td>
		<td class="px-6 py-4 whitespace-nowrap text-sm text-body">
			{ data.Tier }
		</td>
		<td class="px-6 py-4 whitespace-nowrap text-sm text-body">
			{ fmt.Sprintf("%d%%, %d-%d", data.WinPercentage, data.Wins, data.Losses) }
		</td>
	</tr>
}
