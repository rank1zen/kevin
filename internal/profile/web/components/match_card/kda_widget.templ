package match_card

import (
	"context"
	"fmt"
)

type kdaWidgetData struct {
	Kills, Deaths, Assists int
	KillDeathRatio         float32
}

templ kdaWidget(ctx context.Context, data kdaWidgetData) {
	<span class="inline-flex items-center">
		{ fmt.Sprintf("%d / %d / %d", data.Kills, data.Deaths, data.Assists) }
		// <div class="flex flex-none items-center font-medium">
		// 	<div class="flex-1 text-gray-900/90 text-left dark:text-gray-100/90">
		// 	</div>
		// 	<div class="text-gray-900/5 mx-1 dark:text-gray-100/5">
		// 		/
		// 	</div>
		// 	<div class="flex-1 text-red-500/90 text-center">
		// 		{ fmt.Sprintf("%d", data.Deaths) }
		// 	</div>
		// 	<div class="text-gray-900/5 mx-1 dark:text-gray-100/5">
		// 		/
		// 	</div>
		// 	<div class="flex-1 text-gray-900/90 text-right dark:text-gray-100/90">
		// 		{ fmt.Sprintf("%d", data.Assists) }
		// 	</div>
		// </div>
	</span>
}
